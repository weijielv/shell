<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 填充的颜色 -->
     <solid android:color="@android:color/transparent"/>
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <!--<corners android:radius="@dimen/dimen_10px"/>-->
    <stroke android:width="@dimen/divider_height" android:color="@color/c01_fa4532"/>

    <padding
        android:bottom="@dimen/divider_height"
        android:left="@dimen/divider_height"
        android:right="@dimen/divider_height"
        android:top="@dimen/divider_height" />

</shape>
